functionTest1 as function(a as integer, z as boolean): boolean
        declare local locvariable as boolean;
                      x as integer;

        do set x to x + 1
           set locvariable to locvariable + 4

        return locvariable
    done