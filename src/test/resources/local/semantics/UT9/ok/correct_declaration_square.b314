functionTest1 as function(a as integer, z as boolean): square
        declare local locvariable as square[2,2];
                      x as integer;

        do set x to x + 1
           set locvariable to locvariable + 4

        return locvariable
    done