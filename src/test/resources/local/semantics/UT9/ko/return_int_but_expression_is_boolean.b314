functionTest1 as function(a as integer, z as boolean): integer
        declare local locvariable as boolean;
                      x as integer;

        do set x to x + 1
           set locvariable to true

        return locvariable
    done